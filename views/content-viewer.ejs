<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Enhanced PDF Viewer</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700;800&display=swap" rel="stylesheet" />
    <script src="https://unpkg.com/lucide@latest"></script>
</head>

<body class="font-sans bg-gradient-to-br from-blue-50 to-indigo-100 antialiased">

    <style>
        .font-body {
            font-family: 'Inter', sans-serif;
        }

        .font-headline {
            font-family: 'Inter', sans-serif;
            font-weight: 800;
        }

        .pdf-container {
            position: relative;
            overflow: hidden;
        }

        .pdf-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background:
                radial-gradient(circle at 20% 80%, rgba(120, 119, 198, 0.2) 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, rgba(255, 255, 255, 0.1) 0%, transparent 50%);
            pointer-events: none;
        }

        .glass-effect {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            box-shadow:
                0 25px 50px -12px rgba(0, 0, 0, 0.15),
                0 0 0 1px rgba(255, 255, 255, 0.1);
        }

        .pdf-frame {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            border-radius: 16px;
            overflow: hidden;
            transform-origin: center;
        }

        .control-btn {
            transition: all 0.2s ease;
            backdrop-filter: blur(10px);
        }

        .control-btn:hover {
            transform: translateY(-1px);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        .control-btn:active {
            transform: translateY(0);
        }

        .floating-controls {
            position: absolute;
            top: 1rem;
            right: 1rem;
            z-index: 10;
        }

        .mobile-controls {
            position: fixed;
            bottom: 1rem;
            left: 50%;
            transform: translateX(-50%);
            z-index: 60;
        }

        .status-indicator {
            position: absolute;
            bottom: 1rem;
            left: 1rem;
            z-index: 10;
        }

        @keyframes pulse-subtle {

            0%,
            100% {
                opacity: 1;
            }

            50% {
                opacity: 0.7;
            }
        }

        .pulse-subtle {
            animation: pulse-subtle 2s infinite;
        }

        .zoom-controls {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 12px;
            padding: 0.5rem;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
        }

        .zoom-display {
            min-width: 60px;
            text-align: center;
            font-weight: 600;
            font-size: 0.875rem;
            color: #374151;
        }

        /* Responsive iframe container */
        .pdf-iframe-container {
            position: relative;
            width: 100%;
            height: 80vh;
            min-height: 400px;
            max-height: 800px;
        }

        @media (max-width: 768px) {
            .pdf-iframe-container {
                height: 80vh;
                min-height: 350px;
            }

            .floating-controls {
                display: none;
            }

            .glass-effect {
                margin: 0.5rem;
                padding: 1rem;
            }
        }

        @media (max-width: 640px) {
            .pdf-iframe-container {
                height: 75vh;
                min-height: 300px;
            }
        }

        /* Fullscreen styles */
        .fullscreen-mode {
            position: fixed !important;
            top: 0 !important;
            left: 0 !important;
            width: 100vw !important;
            height: 100vh !important;
            z-index: 50 !important;
            margin: 0 !important;
            border-radius: 0 !important;
        }



        .fullscreen-mode .pdf-iframe-container {
            height: calc(100vh - 120px) !important;
            max-height: none !important;
        }

        /* Mobile Fullscreen Fix */
        @media (max-width: 768px) {

   
            #doc-view-header{
                display: none;
            }

            .fullscreen-mode {
                padding: 0 !important;
                /* remove any padding around PDF */
                margin: 0 !important;
            }

            .fullscreen-mode .pdf-iframe-container {
                width: 100vw !important;
                height: 100vh !important;
                /* full viewport height */
                max-height: 100vh !important;
            }

            .pdf-frame {
                transform: none !important;
                /* remove scaling on mobile */
                border-radius: 0 !important;
                /* full screen corners */
            }

            #pdf-iframe {
                width: 100% !important;
                height: 100% !important;
            }
        }
    </style>

    <div class="relative flex min-h-screen flex-col pdf-container">
        <!-- Main Content  -->
        <main class="flex-1 relative">
            <div class="flex items-start justify-center sm:items-center p-2">
                <div class="w-full max-w-7xl">

                    <!-- Enhanced PDF Viewer  -->
                    <div id="pdf-viewer" class="glass-effect rounded-2xl sm:rounded-3xl p-4 sm:p-6 lg:p-8 relative">

                        <!-- Header Section  -->
                        <div id="doc-view-header"
                            class="flex flex-col sm:flex-row items-start sm:items-center justify-between mb-4 sm:mb-6 gap-4">
                            <div class="flex items-center gap-3 sm:gap-4">
                                <div
                                    class="w-10 h-10 bg-black rounded-xl flex items-center justify-center">
                                    <i data-lucide="file-text" class="w-5 h-5 text-white"></i>
                                </div>
                                <div>
                                    <h1 class="text-xl sm:text-2xl font-bold text-gray-800 mb-1">Document Viewer</h1>
                                </div>
                            </div>

                            <!-- Desktop Quick Actions  -->
                            <div class="hidden sm:flex items-center gap-2 lg:gap-3">
                                <button onclick="refreshPDF()"
                                    class="control-btn bg-white/20 hover:bg-white/30 text-gray-700 px-3 lg:px-4 py-2 rounded-xl flex items-center gap-2 border border-white/30 text-sm">
                                    <i data-lucide="refresh-cw" class="w-4 h-4"></i>
                                    <span class="hidden lg:inline">Refresh</span>
                                </button>
                                <button onclick="toggleFullscreen()"
                                    class="control-btn bg-white/20 hover:bg-white/30 text-gray-700 px-3 lg:px-4 py-2 rounded-xl flex items-center gap-2 border border-white/30 text-sm">
                                    <i data-lucide="expand" class="w-4 h-4"></i>
                                    <span class="hidden lg:inline">Fullscreen</span>
                                </button>
                            </div>
                        </div>

                        <!-- PDF Container  -->
                        <div class="relative">
                            <!-- Loader  -->
                            <div id="loading-overlay"
                                class="absolute inset-0 flex items-center justify-center bg-gray-50/95 backdrop-blur-sm z-20 rounded-2xl">
                                <div class="text-center">
                                    <div
                                        class="inline-flex items-center justify-center w-12 h-12 sm:w-16 sm:h-16 bg-blue-100 rounded-full mb-4">
                                        <svg class="w-6 h-6 sm:w-8 sm:h-8 text-blue-600 animate-spin"
                                            xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                                            <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor"
                                                stroke-width="4"></circle>
                                            <path class="opacity-75" fill="currentColor"
                                                d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z">
                                            </path>
                                        </svg>
                                    </div>
                                    <p class="text-gray-600 font-medium text-sm sm:text-base">Loading document...</p>
                                    <p class="text-xs sm:text-sm text-gray-500 mt-1">Please wait while we prepare your
                                        PDF</p>
                                </div>
                            </div>



                            <!-- Status Indicator  -->
                            <div class="status-indicator hidden sm:block">
                                <div
                                    class="bg-white/90 backdrop-blur-sm rounded-full px-3 py-2 flex items-center gap-2 shadow-lg border border-white/30">
                                    <div class="w-2 h-2 bg-green-500 rounded-full pulse-subtle"></div>
                                    <span class="text-xs font-medium text-gray-700">Live Document</span>
                                </div>
                            </div>

                            <!-- Enhanced PDF Frame  -->
                            <div class="pdf-frame bg-white relative group">
                                <div class="pdf-iframe-container">
                                    <iframe id="pdf-iframe"
                                        src="https://drive.google.com/file/d/<%= document %>/preview"
                                        class="w-full h-full border-0 rounded-2xl" onload="hideLoading()"
                                        onerror="showError()" allow="autoplay"
                                        sandbox="allow-same-origin allow-scripts">
                                    </iframe>
                                </div>

                                <!-- Gradient Overlay for Better Integration  -->
                                <div class="absolute inset-0 rounded-2xl pointer-events-none"
                                    style="background: linear-gradient(to bottom, rgba(255,255,255,0.05) 0%, transparent 3%, transparent 97%, rgba(0,0,0,0.02) 100%);">
                                </div>
                            </div>
                        </div>

                    </div>

                    <!-- Error State (Hidden by default)  -->
                    <div id="error-container"
                        class="hidden glass-effect rounded-2xl sm:rounded-3xl p-6 sm:p-8 text-center">
                        <div
                            class="w-12 h-12 sm:w-16 sm:h-16 bg-red-100 rounded-full flex items-center justify-center mx-auto mb-4">
                            <i data-lucide="alert-circle" class="w-6 h-6 sm:w-8 sm:h-8 text-red-500"></i>
                        </div>
                        <h2 class="text-lg sm:text-xl font-bold text-gray-800 mb-2">Unable to Load PDF</h2>
                        <p class="text-sm sm:text-base text-gray-600 mb-6">The document couldn't be loaded. Please check
                            your connection or try again later.</p>
                        <button onclick="retryLoad()"
                            class="bg-gradient-to-r from-blue-500 to-purple-600 hover:from-blue-600 hover:to-purple-700 text-white px-4 sm:px-6 py-2 sm:py-3 rounded-xl flex items-center gap-2 mx-auto shadow-lg transition-all text-sm sm:text-base">
                            <i data-lucide="rotate-ccw" class="w-4 h-4"></i>
                            Try Again
                        </button>
                    </div>
                </div>
            </div>
        </main>

        <!-- Mobile Controls  -->
        <div class="mobile-controls p-4 sm:hidden">
            <div
                class="bg-white/95 backdrop-blur-sm rounded-2xl p-3 shadow-xl border border-white/30 flex items-center gap-3">
                <button onclick="zoomOut()"
                    class="control-btn bg-gray-100 hover:bg-gray-200 text-gray-700 w-10 h-10 rounded-full flex items-center justify-center">
                    <i data-lucide="zoom-out" class="w-4 h-4"></i>
                </button>
                <div class="zoom-display min-w-[50px] text-center">
                    <span id="mobile-zoom-level" class="text-sm font-semibold text-gray-700">100%</span>
                </div>
                <button onclick="zoomIn()"
                    class="control-btn bg-gray-100 hover:bg-gray-200 text-gray-700 w-10 h-10 rounded-full flex items-center justify-center">
                    <i data-lucide="zoom-in" class="w-4 h-4"></i>
                </button>
                <div class="w-px h-6 bg-gray-300 mx-1"></div>
                <button onclick="refreshPDF()"
                    class="control-btn bg-gray-100 hover:bg-gray-200 text-gray-700 w-10 h-10 rounded-full flex items-center justify-center">
                    <i data-lucide="refresh-cw" class="w-4 h-4"></i>
                </button>
                <button onclick="toggleFullscreen()"
                    class="control-btn bg-gray-100 hover:bg-gray-200 text-gray-700 w-10 h-10 rounded-full flex items-center justify-center">
                    <i data-lucide="expand" class="w-4 h-4"></i>
                </button>
            </div>
        </div>
    </div>

    <script>
        lucide.createIcons();
    </script>
    <script src="/js/content-viewer.js"></script>
</body>

</html>