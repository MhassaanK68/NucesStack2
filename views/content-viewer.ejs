<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700;800&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="/css/global.css">
    <script src="https://unpkg.com/lucide@latest"></script>

    <title>NUCES Stack - Centralized Repository for FAST NUCES Course Materials</title>
    <meta name="description" content="NUCES Stack provides course materials, notes, and resources for FAST NUCES students. Browse by semester and subject to access everything you need for your studies.">
    <meta name="keywords" content="NUCES, FAST NUCES, course materials, notes, programming, physics, calculus, university resources">
    <meta name="author" content="NUCES Stack Team">
    <meta name="robots" content="index, follow">

    <!-- Canonical URL -->
    <link rel="canonical" href="https://nucesstack.site/">

    <!-- Open Graph (works for WhatsApp, Facebook, LinkedIn, Slack, etc.) -->
    <meta property="og:title" content="NUCES Stack - FAST NUCES Course Materials">
    <meta property="og:description" content="Centralized repository for FAST NUCES course notes, assignments, and study resources.">
    <meta property="og:url" content="https://nucesstack.site/">
    <meta property="og:type" content="website">
    <meta property="og:site_name" content="NUCES Stack">
    <meta property="og:locale" content="en_PK">
    <meta property="og:image" content="https://nucesstack.site/preview-image.png">
    <meta property="og:image:secure_url" content="https://nucesstack.site/preview-image.png">
    <meta property="og:image:type" content="image/png">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">

    <!-- Twitter Card (large preview) -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="NUCES Stack - FAST NUCES Course Materials">
    <meta name="twitter:description" content="Find all FAST NUCES course resources in one place. Organized by semester and subject.">
    <meta name="twitter:image" content="https://nucesstack.site/preview-image.png">
    <meta name="twitter:site" content="@nucesstack">
    <meta name="twitter:creator" content="@nucesstack">

    <!-- Favicon -->
    <link rel="icon" href="/images/favicon.ico" type="image/x-icon">



</head>

<body class="font-sans bg-gradient-to-br from-blue-50 to-indigo-100 antialiased">

    <style>
        .font-body {
            font-family: 'Inter', sans-serif;
        }

        .font-headline {
            font-family: 'Inter', sans-serif;
            font-weight: 800;
        }

        .pdf-container {
            position: relative;
            overflow: hidden;
        }

        .pdf-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background:
                radial-gradient(circle at 20% 80%, rgba(120, 119, 198, 0.2) 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, rgba(255, 255, 255, 0.1) 0%, transparent 50%);
            pointer-events: none;
        }

        .glass-effect {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            box-shadow:
                0 25px 50px -12px rgba(0, 0, 0, 0.15),
                0 0 0 1px rgba(255, 255, 255, 0.1);
        }

        .pdf-frame {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            border-radius: 0px;
            overflow: hidden;
            transform-origin: center;
        }

        .control-btn {
            transition: all 0.2s ease;
            backdrop-filter: blur(10px);
        }

        .control-btn:hover {
            transform: translateY(-1px);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2);
        }

        .control-btn:active {
            transform: translateY(0);
        }

        .floating-controls {
            position: absolute;
            top: 1rem;
            right: 1rem;
            z-index: 10;
        }

        .mobile-controls {
            position: fixed;
            bottom: 1rem;
            left: 50%;
            transform: translateX(-50%);
            z-index: 60;
        }

        .status-indicator {
            position: absolute;
            bottom: 1rem;
            left: 1rem;
            z-index: 10;
        }

        @keyframes pulse-subtle {

            0%,
            100% {
                opacity: 1;
            }

            50% {
                opacity: 0.7;
            }
        }

        .pulse-subtle {
            animation: pulse-subtle 2s infinite;
        }

        .zoom-controls {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 12px;
            padding: 0.5rem;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
        }

        .zoom-display {
            min-width: 60px;
            text-align: center;
            font-weight: 600;
            font-size: 0.875rem;
            color: #374151;
        }

        /* Responsive iframe container */
        .pdf-iframe-container {
            position: relative;
            width: 100%;
            height: 80vh;
            min-height: 400px;
            max-height: 800px;
        }

        @media (max-width: 768px) {
            .pdf-iframe-container {
                height: 80vh;
                min-height: 350px;
            }

            .floating-controls {
                display: none;
            }

            .glass-effect {
                margin: 0.5rem;
                padding: 1rem;
            }
        }

        @media (max-width: 640px) {
            .pdf-iframe-container {
                height: 75vh;
                min-height: 300px;
            }
        }

        /* Fullscreen styles */
        .fullscreen-mode {
            position: fixed !important;
            top: 0 !important;
            left: 0 !important;
            width: 100vw !important;
            height: 100vh !important;
            z-index: 9999 !important;
            margin: 0 !important;
            padding: 0 !important;
            border-radius: 0 !important;
        }

        .fullscreen-mode .pdf-iframe-container {
            width: 100vw !important;
            height: 100vh !important;
            max-height: 100vh !important;
        }

        .fullscreen-mode iframe {
            width: 100% !important;
            height: 100% !important;
            border-radius: 0 !important;
        }


        /* Mobile Fullscreen Fix */
        @media (max-width: 768px) {


            .fullscreen-mode {
                padding: 0 !important;
                margin: 0 !important;
            }

            .fullscreen-mode .pdf-iframe-container {
                width: 100vw !important;
                height: 100vh !important;
                /* full viewport height */
                max-height: 100vh !important;
            }

            .pdf-frame {
                transform: none !important;
                /* remove scaling on mobile */
                border-radius: 0 !important;
                /* full screen corners */
            }

            #pdf-iframe {
                width: 100% !important;
                height: 100% !important;
            }
        }
    </style>

    <div class="relative flex min-h-screen flex-col pdf-container">

        <header
            class="sticky top-0 z-50 w-full border-b border-gray-300 color-primary bg-primary backdrop-blur">
            <div class="container mx-auto flex h-14 items-center px-4">
                <a class="flex items-center gap-2 font-bold text-lg mr-6" href="/">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 color-primary" fill="none"
                        viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                        <path d="M2 16V4a2 2 0 0 1 2-2h11"></path>
                        <path
                            d="M22 18H11a2 2 0 1 0 0 4h10.5a.5.5 0 0 0 .5-.5v-15a.5.5 0 0 0-.5-.5H11a2 2 0 0 0-2 2v12">
                        </path>
                        <path d="M5 14H4a2 2 0 1 0 0 4h1"></path>
                    </svg>
                    <span class="font-headline">Nuces Stack</span>
                </a>
                <div class="flex flex-1 items-center justify-end">
                    <nav class="flex items-center">
                        <a href="/admin">
                            <button
                                class="text-black bg-primary inline-flex items-center gap-2 text-sm font-medium border border-gray-300 hover:bg-gray-100 h-9 rounded-md px-3">
                                <svg xmlns="http://www.w3.org/2000/svg" class="mr-2 h-4 w-4" fill="none"
                                    viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                                    <circle cx="18" cy="15" r="3"></circle>
                                    <circle cx="9" cy="7" r="4"></circle>
                                    <path d="M10 15H6a4 4 0 0 0-4 4v2"></path>
                                </svg>
                                Admin Panel
                            </button>
                        </a>
                    </nav>
                </div>
            </div>
        </header>


        <!-- Main Content  -->
        <main class="flex-1 relative">
            <div class="flex items-start justify-center sm:items-center p-2">
                <div class="w-full max-w-7xl">

                    <!-- Enhanced PDF Viewer  -->
                    <div id="pdf-viewer" class="glass-effect p-4 sm:p-6 lg:p-8 relative">

                        <!-- Floating Controls -->
                        <div class="floating-controls flex gap-2">
                            <button onclick="refreshPDF()"
                                class="control-btn bg-white/90 hover:bg-white text-gray-700 px-3 py-2 rounded-xl flex items-center gap-2 border border-gray-300 shadow-md">
                                <i data-lucide="refresh-cw" class="w-4 h-4"></i>
                            </button>
                            <button onclick="toggleFullscreen()"
                                class="control-btn bg-white/90 hover:bg-white text-gray-700 px-3 py-2 rounded-xl flex items-center gap-2 border border-gray-300 shadow-md">
                                <i data-lucide="expand" class="w-4 h-4"></i>
                            </button>
                        </div>


                        <!-- PDF Container  -->
                        <div class="relative">
                            <!-- Loader  -->
                            <div id="loading-overlay"
                                class="absolute inset-0 flex items-center justify-center bg-gray-50/95 backdrop-blur-sm z-20 rounded-2xl">
                                <div class="text-center">
                                    <div
                                        class="inline-flex items-center justify-center w-12 h-12 sm:w-16 sm:h-16 bg-blue-100 rounded-full mb-4">
                                        <svg class="w-6 h-6 sm:w-8 sm:h-8 text-blue-600 animate-spin"
                                            xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                                            <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor"
                                                stroke-width="4"></circle>
                                            <path class="opacity-75" fill="currentColor"
                                                d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z">
                                            </path>
                                        </svg>
                                    </div>
                                    <p class="text-gray-600 font-medium text-sm sm:text-base">Loading document...</p>
                                    <p class="text-xs sm:text-sm text-gray-500 mt-1">Please wait while we prepare your
                                        PDF</p>
                                </div>
                            </div>



                            <!-- Status Indicator  -->
                            <div class="status-indicator hidden sm:block">
                                <div
                                    class="bg-white/90 backdrop-blur-sm rounded-full px-3 py-2 flex items-center gap-2 shadow-lg border border-white/30">
                                    <div class="w-2 h-2 bg-green-500 rounded-full pulse-subtle"></div>
                                    <span class="text-xs font-medium text-gray-700">Live Document</span>
                                </div>
                            </div>

                            <!-- Enhanced PDF Frame  -->
                            <div class="pdf-frame bg-white relative group">
                                <div class="pdf-iframe-container">
                                    <iframe id="pdf-iframe"
                                        src="https://drive.google.com/file/d/<%= document %>/preview"
                                        class="w-full h-full border-0" onload="hideLoading()"
                                        onerror="showError()" allow="autoplay"
                                        sandbox="allow-same-origin allow-scripts">
                                    </iframe>
                                </div>

                                <!-- Gradient Overlay for Better Integration  -->
                                <div class="absolute inset-0 rounded-2xl pointer-events-none"
                                    style="background: linear-gradient(to bottom, rgba(255,255,255,0.05) 0%, transparent 3%, transparent 97%, rgba(0,0,0,0.02) 100%);">
                                </div>
                            </div>
                        </div>

                    </div>

                    <!-- Error State (Hidden by default)  -->
                    <div id="error-container"
                        class="hidden glass-effect rounded-2xl sm:rounded-3xl p-6 sm:p-8 text-center">
                        <div
                            class="w-12 h-12 sm:w-16 sm:h-16 bg-red-100 rounded-full flex items-center justify-center mx-auto mb-4">
                            <i data-lucide="alert-circle" class="w-6 h-6 sm:w-8 sm:h-8 text-red-500"></i>
                        </div>
                        <h2 class="text-lg sm:text-xl font-bold text-gray-800 mb-2">Unable to Load PDF</h2>
                        <p class="text-sm sm:text-base text-gray-600 mb-6">The document couldn't be loaded. Please check
                            your connection or try again later.</p>
                        <button onclick="retryLoad()"
                            class="bg-gradient-to-r from-blue-500 to-purple-600 hover:from-blue-600 hover:to-purple-700 text-white px-4 sm:px-6 py-2 sm:py-3 rounded-xl flex items-center gap-2 mx-auto shadow-lg transition-all text-sm sm:text-base">
                            <i data-lucide="rotate-ccw" class="w-4 h-4"></i>
                            Try Again
                        </button>
                    </div>
                </div>
            </div>
        </main>

        <!-- Mobile Controls  -->
        <div class="mobile-controls p-4 sm:hidden">
            <div
                class="bg-white/95 backdrop-blur-sm rounded-2xl p-3 shadow-xl border border-white/30 flex items-center gap-3">
                <div class="w-px h-6 bg-gray-300 mx-1"></div>
                <button onclick="refreshPDF()"
                    class="control-btn bg-gray-100 hover:bg-gray-200 text-gray-700 w-10 h-10 rounded-full flex items-center justify-center">
                    <i data-lucide="refresh-cw" class="w-4 h-4"></i>
                </button>
                <button onclick="toggleFullscreen()"
                    class="control-btn bg-gray-100 hover:bg-gray-200 text-gray-700 w-10 h-10 rounded-full flex items-center justify-center">
                    <i data-lucide="expand" class="w-4 h-4"></i>
                </button>
            </div>
        </div>
    </div>

    <script>
        lucide.createIcons();
    </script>
    <script src="/js/content-viewer.js"></script>
</body>

</html>