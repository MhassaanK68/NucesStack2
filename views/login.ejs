<%- include('partials/header') %>
    <!-- Sign In Form -->
    <div class="flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
        <div class="max-w-md w-full space-y-8">

            <div class="bg-white p-8 rounded-2xl shadow-lg border border-slate-200">

                <div class="flex items-center justify-center border-b py-6">
                    <a class="flex items-center gap-2 font-bold text-lg mr-6" >
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-10 w-10 color-primary" fill="none"
                            viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                            <path d="M2 16V4a2 2 0 0 1 2-2h11"></path>
                            <path
                                d="M22 18H11a2 2 0 1 0 0 4h10.5a.5.5 0 0 0 .5-.5v-15a.5.5 0 0 0-.5-.5H11a2 2 0 0 0-2 2v12">
                            </path>
                            <path d="M5 14H4a2 2 0 1 0 0 4h1"></path>
                        </svg>
                        <span class="color-primary text-3xl">Nuces Stack</span>
                    </a>
                </div>

                <!-- Form -->
                <form method="post" action="/login" class="mt-6 space-y-6 ">
                    <div class="space-y-4">
                        <%if (error) {%>
                            <span class="text-red-500">Invalid Credentials</span>
                        <%}%>
                        <!-- Email Field -->
                        <div>
                            <label for="username" class="block text-sm font-medium text-slate-700 mb-2">
                                username
                            </label>
                            <input 
                                id="username" 
                                name="username" 
                                type="text" 
                                required 
                                class="w-full px-4 py-3 border border-slate-300 rounded-lg focus:ring-2 focus:ring-cyan-800 focus:border-transparent transition-all duration-200 placeholder-slate-400"
                                placeholder="Enter your username"
                            >
                        </div>

                        <!-- Password Field -->
                        <div>
                            <label for="password" class="block text-sm font-medium text-slate-700 mb-2">
                                Password
                            </label>
                            <input 
                                id="password" 
                                name="password" 
                                type="password" 
                                required 
                                class="w-full px-4 py-3 border border-slate-300 rounded-lg focus:ring-2 focus:ring-cyan-800 focus:border-transparent transition-all duration-200 placeholder-slate-400"
                                placeholder="Enter your password"
                            >
                        </div>
                    </div>


                    <!-- Sign In Button -->
                    <div>
                        <button 
                            type="submit" 
                            class="group relative w-full flex justify-center py-3 px-4 border border-transparent text-sm font-medium rounded-lg text-white bg-gradient-to-r from-cyan-800 to-cyan-900 hover:from-cyan-900 hover:to-cyan-800 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-cyan-800 transition-all duration-200 shadow-lg hover:shadow-xl transform hover:-translate-y-0.5"
                        >
                            Sign in to your account
                        </button>
                    </div>

                </form>

            </div>
        </div>
    </div>

<%- include('partials/footer') %>
